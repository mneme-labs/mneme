# MnemeCache secrets — EDIT BEFORE APPLYING
# Generate strong random values:
#   openssl rand -base64 32   (for cluster_secret and admin_password)
#
# Values below are base64-encoded. Encode your own:
#   echo -n "my-secret" | base64
apiVersion: v1
kind: Secret
metadata:
  name: mneme-auth
  namespace: mneme
  labels:
    app.kubernetes.io/name: mneme
    app.kubernetes.io/component: auth
type: Opaque
data:
  # base64("change-me-in-production")
  cluster_secret: Y2hhbmdlLW1lLWluLXByb2R1Y3Rpb24=
  # base64("admin-password-here")
  admin_password: YWRtaW4tcGFzc3dvcmQtaGVyZQ==
